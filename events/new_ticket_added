sub handle_new_ticket_added{
	my $self = shift;
	my $data = shift; # data will be a hashref

	foreach my $callback (@new_ticket_added_callbacks){
		&{$callback}($data);
	}
	# do stuff

	return 1; # for success
}
